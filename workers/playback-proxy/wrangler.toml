name = "click-tv-playback-proxy"
main = "src/index.js"
compatibility_date = "2026-07-31"
workers_dev = true

# No variables, secrets or KV bindings are required. Deploy the same source to
# all four Workers. The production site/catalogue addresses and allowed origin
# are intentionally fixed in src/index.js.
