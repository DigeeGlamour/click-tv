================================================================================
CODEX SIX POINT — FINAL VERIFICATION REPORT
প্রতিটি দাবি কোডে মেপে যাচাই | কী সারালাম | কী pass করল | প্রমাণ সহ
================================================================================

তারিখ       : 2026-08-27
Repository  : DigeeGlamour/click-tv, branch main
test suite  : 1301 tests, OK (244.781s)
              (CI-র নিজের command: python -m unittest discover -s tests -p "test_*.py")

Codex ৬টি পয়েন্ট দিয়েছিল। প্রতিটি কোডে গিয়ে reproduce করে যাচাই করা হয়েছে —
কোনোটি না দেখে মেনে নেওয়া হয়নি, না দেখে অস্বীকারও করা হয়নি।

  ৩টি আগের পর্বেই সারানো ছিল  →  এবার CI-তে প্রমাণ পাওয়া গেল
  ২টি সত্যিই বাকি ছিল          →  এই পর্বে সারানো
  ১টি আংশিক                    →  যেটা এখানে সম্ভব করা হলো, বাকিটা হাতে নেই

পথে আমার নিজের আরও ২টি ভুল ধরা পড়ল, দুটোই সারানো।

Codex-এর রায় ছিল: "Zee auto-scan durability এবং intelligent two-vantage
enforcement এখনো ১০০% complete নয়।" — সেই দুটোর মধ্যে enforcement অংশটি এই
পর্বে সম্পূর্ণ হয়েছে (ভাগ ১)। durability অংশে code সম্পূর্ণ, কিন্তু Zee-র
proven route নিজেই এখন মৃত (ভাগ ৭.২) — সেটি code নয়, upstream সমস্যা।


================================================================================
০. এক নজরে
================================================================================

  #  | Codex-এর দাবি                            | রায়       | অবস্থা
  ---+------------------------------------------+----------+--------------------
  ১  | preference top-6 selection-এর পরে চলে    | সঠিক ছিল | আগেই সারানো ✅
  ২ক | preferred route-এর expiry নেই            | সঠিক ছিল | আগেই সারানো ✅
  ২খ | current-health check নেই                 | সঠিক     | এই পর্বে সারানো ✅
  ৩  | দুই-vantage/persistence বাস্তবে হয় না     | সঠিক ছিল | CI-তে প্রমাণিত ✅
  ৪  | evidence hide path-এর পরে দেওয়া হয়        | **সঠিক** | এই পর্বে সারানো ✅
  ৫  | HMAC নিয়ে report পুরোনো তথ্য              | সঠিক     | CI-তে প্রমাণিত ✅
  ৬  | BD vantage + device playback test বাকি    | সঠিক     | device করা ✅
     |                                          |          | BD হাতে নেই ⚠️


================================================================================
১. পয়েন্ট ৪ — evidence hide path-এর পরে দেওয়া হত  ✅ সারানো
================================================================================
এটিই সবচেয়ে গুরুতর, এবং Codex শুধু line number পড়েই ধরেছে।

১.১  reproduce করে যাচাই
--------------------------------------------------------------------------------
আগের অবস্থা, scanner/fast_pipeline.py:

    verify_bd_stream ডাকা হয়  : line 1476, 1845, 1949
    evidence সরবরাহ           : line 1974   ← সবার পরে

অর্থাৎ bd_verifier যখন item hide করে, model-এর কাছে তখন কিছুই নেই। সরাসরি
চালিয়ে মাপা:

    model_permits_hide('bd_verifier.confirmed_permanent_http', item) → True
    কারণ: "no per-route evidence for this item; caller decision stands"

আগের পর্বে bd_verifier-এর ৫টি site `audit_hide_safe` → `model_permits_hide`
বদলে "সারানো হয়েছে" লিখেছিলাম। gate ছিল — **কিন্তু কিছুই আটকাতে পারত না**,
কারণ evidence তখনো আসেনি। Codex-এর ধরাটা সঠিক ছিল।

১.২  সমাধান, এবং কেন এটা এখন সম্ভব হলো
--------------------------------------------------------------------------------
cross-scan persistence (পয়েন্ট ৩-এর কাজ) থাকার কারণেই ক্রমটা ঠিক করা গেল:
আগের scan-এর observation disk-এ থাকে, তাই এই scan **শুরুতেই** সেটা model-কে
দিতে পারে — যেকোনো verification-এর আগে।

নতুন function: `_load_cached_evidence_before_verification()`  (line 242)

    preload evidence         : line 1221   ← এখন সবার আগে
    প্রথম verify_bd_stream    : line 1476
    fresh observation supply : line 1974

এই scan-এর নিজের observation শেষেই cache-এ যায়, **পরের run-এর জন্য**। একটি
scan নিজের observation দিয়ে নিজেকেই দ্বিতীয় vantage হিসেবে গণ্য করবে না —
করলে single-vantage single-window ভুলটাই নতুন পোশাকে ফিরে আসত।

১.৩  মাপা প্রমাণ
--------------------------------------------------------------------------------
cache-এ আগের scan-এর 403 evidence (দুই vantage, দুই আলাদা window) রেখে:

    preload করা record : 4
    bd_verifier hide করতে পারে? False
    কারণ: "fewer than two complete, escalatable, globally scoped observations"

আর সত্যিকারের route failure দিলে hide **অনুমোদিত** — gate অন্ধভাবে সব
আটকায় না, সেটাও মাপা।

test: tests/test_evidence_ordering.py — ৯টি
  ক্রমটাই assert করা (preload-এর line number < verify_bd_stream-এর line
  number), তাই ভবিষ্যতে কেউ উল্টে দিলে test fail করবে। সঙ্গে আচরণ: 403
  আটকায়, আসল failure অনুমোদন পায়, historical evidence দুবার সরবরাহ হয় না,
  ভাঙা cache scan ভাঙে না।


================================================================================
২. পয়েন্ট ২খ — preferred route-এর current-health check  ✅ সারানো
================================================================================
Codex-এর ২ নম্বর পয়েন্টের দুটি অংশ ছিল। expiry (২ক) আগেই সারানো — ১৪ দিনের
TTL, timestamp ছাড়া entry-ও stale ধরা হয়। কিন্তু দ্বিতীয় অংশ বাকি ছিল:
TTL-এর **ভেতরে** থাকা preference এমন route নির্দেশ করতে পারত যা ইতিমধ্যে
নষ্ট, আর কেউ current scan-কে জিজ্ঞেসই করত না। Codex ঠিক এটাই বলেছিল:
"intelligent fallback needs current-scan health"।

নতুন: `route_preference._is_healthy_this_scan()`

ইচ্ছাকৃতভাবে **শুধু একটি জিনিসে কঠোর** — স্পষ্ট নিষেধ। `publish_allowed is
False` হলো এই প্রকল্পের প্রতিটি hide path-এর "এটা পরিবেশন করো না" লেখার
উপায়, তাই সেটি থাকলে পুরোনো proof আর জিততে পারবে না। বাকি সব অবস্থা
(verified, pending, geo-protected, কোনো mark নেই) ছাড় পায় — কারণ অনুপস্থিত
field-কে "অসুস্থ" পড়লে বেশিরভাগ route-এ promotion বন্ধ হয়ে যেত, আর তাতে এই
registry-র উদ্দেশ্যই নষ্ট হত।

মাপা যাচাই:

    সুস্থ route promote হয়                        True
    publish_allowed=False → promote                False
    metadata_only=True    → promote                False
    url খালি              → promote                False
    কোনো flag নেই (সাধারণ ক্ষেত্র) → promote        True
    full_pool থেকেও denied route টানা হয় না         False

test: tests/test_route_preference_gaps.py — মোট ১৯টি
  SlotTruncationTests ৬  (পয়েন্ট ১)
  StalenessTests      ৬  (পয়েন্ট ২ক)
  CurrentHealthTests  ৭  (পয়েন্ট ২খ, নতুন)
expiry ও health দুটি স্বাধীন guard — কোনোটি অন্যটির উপর নির্ভর করে না,
সেটাও assert করা।


================================================================================
৩. পয়েন্ট ১, ৩, ৫ — আগেই সারানো, এবার CI-তে প্রমাণ
================================================================================

৩.১  পয়েন্ট ১ — slot truncation
--------------------------------------------------------------------------------
    scanner/merger.py:1569   full_pool=protocol_candidates

`protocol_candidates` হলো max_total কাটার **আগের** পুরো pool, তাই ৭ম স্থানে
থাকা proven route এখন দেখা যায়। Codex-এর ৭-candidate reproduction আমি
নিজেও চালিয়েছি:

    fix-এর আগে : primary = rival0.example.net  (ভাঙা route)
    fix-এর পরে : primary = proven route        (৭ম স্থান থেকে উঠে এলো)

promote হলে `previous_primary_identity` খালি করা হয়, নইলে incumbent-hold
block পরের লাইনেই promotion উল্টে দিত।

৩.২  পয়েন্ট ৩ — দুই vantage ও persistence, CI-তে মাপা
--------------------------------------------------------------------------------
Codex বলেছিল scanner-এ `proxy_http_status` আছে কিন্তু ব্যবহার হয় না, আর
evidence পরের scan-এর জন্য থাকে না — তাই production-এ দুই-vantage শর্ত
কখনো পূরণ হবে না। এখন CI-র নিজের scan যা তৈরি করেছে:

    state/route-evidence-cache.json
      route            : 21956
      মোট record       : 22846
      scanner_egress : 22326 record
      proxy_egress   : 520 record
      দুই vantage আছে  : 330 route

`proxy_egress` record-এর অস্তিত্বই প্রমাণ যে `proxy_http_status` সত্যিই পড়া
হচ্ছে, আর file-টি CI-তে committed হওয়াই প্রমাণ যে evidence পরের scan পর্যন্ত
টেকে। ৩৩০টি route-এ দুই vantage মিলে গেছে — অর্থাৎ শর্তটি তত্ত্ব নয়,
production-এ পূরণ হচ্ছে।

৩.৩  পয়েন্ট ৫ — HMAC, CI-তে প্রমাণিত
--------------------------------------------------------------------------------
Codex ঠিক বলেছিল যে আমার আগের report ভবিষ্যৎকালে লিখেছিল ("পরের scan-এ যাচাই
হবে") অথচ ঘটনা ইতিমধ্যে ঘটে গেছে। এখন reports/visibility-model-audit.json:

    hmac_key.configured : True
    hmac_key.key_id     : key-2026-08
    decisions_seen      : 20992
    model_would_hide    : 378

cache-এর প্রতিটি record-এ `hmac_key_id: key-2026-08` — অর্থাৎ নতুন
integration code CI-তে key নিয়ে চলেছে, পুরোনো code নয়।

Codex-এর দ্বিতীয় অংশও সঠিক ছিল ("নতুন integration commit-এর পরে full channel
auto-scan হয়নি")। **এখন হয়েছে** — commit 17669311d, "Auto update: channels
[2026-08-26 20:14 UTC]", আমার fix commit f599d440b (08-26 19:15)-এর পরে।

আর CI এখন টানা success:

    2026-08-27T04:29  completed  success   9m44s
    2026-08-27T03:30  completed  success  18m51s
    2026-08-27T00:03  completed  success   4m26s
    2026-08-26T23:45  completed  success   9m00s
    2026-08-26T20:36  completed  success   4m40s
    2026-08-26T20:13  completed  success   8m39s

এর আগে টানা ৮টি failure ছিল — কারণ ছিল আমার নিজের test (ভাগ ৫)।


================================================================================
৪. পয়েন্ট ৬ — DEVICE PLAYBACK TEST
================================================================================
Codex ঠিক বলেছিল দুটোই বাকি। একটি এখানে করা সম্ভব ছিল, সেটা করা হলো।

৪.১  প্রথমে একটি লুকানো সমস্যা ধরা পড়ল
--------------------------------------------------------------------------------
`scripts/sustained-playback-check.py`-এ `--profile` ছিল **শুধু একটি label**
যা report-এ লেখা হত। browser প্রতিবারই desktop Chromium। অর্থাৎ
`--profile android_chrome` দিয়ে চালালে report-এ "Android-এ test হয়েছে" লেখা
হত, অথচ Android সম্পর্কে **কিছুই মাপা হত না**।

এটি test না চালানোর চেয়েও খারাপ — মিথ্যা প্রমাণ তৈরি হত, যা পরে কেউ
বিশ্বাস করে সিদ্ধান্ত নিত। তাই আগে emulation ঠিক করা হলো, তারপর চালানো:

    profile          engine    viewport     user agent
    ---------------  --------  -----------  ---------------------------------
    desktop_chrome   chromium  1366x768     desktop Chrome
    android_chrome   chromium   412x915     Pixel 8, touch
    iphone_safari    webkit     390x844     iPhone OS 17.5, touch
    android_tv       chromium  1920x1080    BRAVIA 4K SmartTV

engine মেলানো এখানে বিশেষভাবে জরুরি, কারণ প্রশ্নটাই codec/container support
নিয়ে — আর এই প্রকল্পে ইতিমধ্যে মাপা আছে যে HEVC Chromium-এ নেই কিন্তু
WebKit-এ আছে। iPhone-কে Chromium দিয়ে test করলে ঠিক এই পার্থক্যটাই হারিয়ে
যেত।

৪.২  Zee Bangla-তে device playback ফল  (১২০ সেকেন্ড × ২ session প্রতিটিতে)
--------------------------------------------------------------------------------
  android_chrome  (chromium, 412x915)
    session 0 : advisory:device_or_browser_unsupported
                startup=শুরুই হয়নি  media progress=0s  fatal=media element error code 3
    session 1 : advisory:device_or_browser_unsupported
                startup=শুরুই হয়নি  media progress=0s  fatal=media element error code 3
    proven: 0 / 1 route
    file  : reports/zee-device-android_chrome.json

  iphone_safari  (webkit, 390x844)
    session 0 : advisory:device_or_browser_unsupported
                startup=শুরুই হয়নি  media progress=0s  fatal=media element error code 3
    session 1 : advisory:device_or_browser_unsupported
                startup=শুরুই হয়নি  media progress=0s  fatal=media element error code 3
    proven: 0 / 1 route
    file  : reports/zee-device-iphone_safari.json

  android_tv  (chromium, 1920x1080)
    session 0 : advisory:device_or_browser_unsupported
                startup=4.4s  media progress=0.08s  fatal=media element error code 3
    session 1 : advisory:device_or_browser_unsupported
                startup=4.4s  media progress=0.08s  fatal=media element error code 3
    proven: 0 / 1 route
    file  : reports/zee-device-android_tv.json

সিদ্ধান্ত: তিনটি device-এই একই `media element error code 3`
(MEDIA_ERR_DECODE)। অর্থাৎ Zee Bangla-র decode ব্যর্থতা **device-নির্দিষ্ট
নয়** — interlaced H.264 ও শূন্য IDR frame প্রতিটি decoder-এর জন্যই সমস্যা।

একটি লক্ষণীয় পার্থক্য আছে, এবং তা এই সিদ্ধান্তকে দুর্বল করে না বরং জোরালো
করে: Android TV-তে startup ৪.৪ সেকেন্ডে **হয়েছিল**, কিন্তু media progress
মাত্র ০.০৮ সেকেন্ড — অর্থাৎ প্রথম frame ধরার পরপরই decoder আটকে গেছে।
মোবাইলে playback শুরুই হয়নি। ব্যর্থতার সময় ভিন্ন, কারণ এক — কোনো decoder-ই
random-access point ছাড়া চালিয়ে যেতে পারছে না।

এটি আগে শুধু যুক্তি ছিল ("গঠনগত সমস্যা, তাই সব device-এ থাকবে")। এখন মাপা,
৬টি স্বাধীন session-এ, তিনটি ভিন্ন engine/viewport-এ।

সব verdict `advisory:` — অর্থাৎ non-escalatable, কোনো channel এতে hide হবে
না। device-এর সীমাবদ্ধতা global রায় হিসেবে গণ্য হচ্ছে না, যা `verdict_scope`
নকশার উদ্দেশ্য।

৪.৩  ২১১টি movie — BD/residential vantage আমার হাতে নেই
--------------------------------------------------------------------------------
scanner egress = AS8075 Microsoft Azure (datacenter)
proxy   egress = AS13335 Cloudflare      (datacenter)

দুটোই datacenter। তাই ২১১টি movie-র 403 আসলে geo-block না মৃত route — এখান
থেকে জানার উপায় নেই, এবং অনুমান করে লিখলে সেটা প্রমাণ নয়। লাগবে বাংলাদেশ
বা residential IP থেকে একটি probe। script লিখে দিতে পারি, চালাতে হবে ওই
network থেকে।


================================================================================
৫. পথে আমার নিজের ২টি ভুল, দুটোই সারানো
================================================================================

ভুল ১ — `--profile` শুধু label ছিল  (ভাগ ৪.১)
  device test চালালে মিথ্যা প্রমাণ তৈরি হত। emulation ঠিক করে তারপর চালানো
  হলো।

ভুল ২ — আমার একটি test `publish_allowed` শব্দটাই নিষিদ্ধ করেছিল
  `test_it_can_only_promote_never_hide` পুরো ফাইলে substring খুঁজত। কিন্তু
  নতুন health check ওই field **পড়ে** — hide flag দেখে promotion থেকে বিরত
  থাকার জন্য, যা hide-এর ঠিক উল্টো। test সঠিক fix-কেই আটকে দিচ্ছিল
  (Ran 1300, failures=1)।

  এখন AST দিয়ে **লেখা** যাচাই করে: subscript store, attribute store, এবং
  `update(publish_allowed=...)`-এর মতো call keyword — সবই ধরা পড়বে, কিন্তু
  পড়া নয়। সঙ্গে একটি নতুন test নিশ্চিত করে health check-এর ওই পড়াটা আছে,
  যাতে কেউ মুছে দিলে ধরা পড়ে।

  এর আগের পর্বে আরও বড় একটি একই জাতের ভুল সারানো হয়েছিল: তিনটি test
  `data/channels/indian.json`-এর **বর্তমান বিষয়বস্তু** assert করত। jio-tv
  worker HTTP 530 দেওয়া শুরু করলে scanner ঠিক কাজই করেছিল (মৃত route বাদ
  দিয়েছে), কিন্তু আমার test সেই সঠিক আচরণকে failure বলেছিল — টানা ৮টি CI
  run ভেঙেছে। শিক্ষা একটাই: test যেন guard ও registry যাচাই করে, upstream
  network-এর মুহূর্তের অবস্থা নয়।


================================================================================
৬. এই পর্বে যা বদলাল
================================================================================

code:
  scanner/fast_pipeline.py             _load_cached_evidence_before_verification()
                                       line 242, ডাকা হয় line 1221 — সব hide
                                       path-এর আগে
  scanner/route_preference.py          _is_healthy_this_scan()
  scripts/sustained-playback-check.py  ৪টি profile-এ আসল device emulation
                                       (engine, viewport, UA, touch) + report-এ
                                       browser_engine ও viewport লেখা

নতুন test:
  tests/test_evidence_ordering.py           ৯টি

বদলানো test:
  tests/test_route_preference_gaps.py       +৭টি (CurrentHealthTests)
  tests/test_route_preference.py            never-hide test AST-ভিত্তিক +১টি

নতুন প্রমাণ-file:
  reports/zee-device-android_chrome.json
  reports/zee-device-iphone_safari.json
  reports/zee-device-android_tv.json


================================================================================
৭. এখনো যা বাকি — সৎ তালিকা
================================================================================

৭.১  ২১১টি movie-র BD/residential যাচাই
     vantage আমার হাতে নেই (ভাগ ৪.৩)। এটি করা যায়নি, এবং করা হয়েছে বলা
     হবে না।

৭.২  Zee Bangla-র একটি জীবিত proven route
     ৮টি সত্যিকারের candidate যাচাই করা হয়েছিল (Cinema ও Sonar বাদ, কারণ
     সেগুলো আলাদা channel — আপনার নিয়ম অনুযায়ী):
       ৭টি মৃত  : 403 ×৩, 404 ×১, 530 ×২, DNS ব্যর্থ ×১
       ১টি জীবিত : 103.159.180.34 (HLS) — কিন্তু proxy দিয়ে manifest load
                   করতে পারেনি, ৪টি session-এ ০/১ proven

     card এখন   : rgkkw.live / mpegts / verified_global / 0 backup
     registry-তে route_id অটুট আছে:
       jio-tv.sm-monirul-islam-rs.workers.dev/live.m3u8?id=625
       pass_count=2, window=120.0s

     অর্থাৎ worker ফিরে এলে merger নিজেই ওটাকে primary করবে — কোনো code
     পরিবর্তন ছাড়াই। এটিই এই পুরো registry-র উদ্দেশ্য ছিল, এবং এখন সেটি
     পরীক্ষিত: route মরার পরেও proof হারায়নি, আবার মৃত route জোর করে
     ঢোকানোও হয়নি (ভাগ ২-এর health check)।

৭.৩  নতুন ordering fix-এর CI প্রমাণ
     এই commit-এর পরে যে channels auto-scan হবে, তার
     state/route-evidence-cache.json ও audit report দেখে নিশ্চিত হওয়া যাবে।
     workflow_dispatch manually চালানোর অনুমতি আমার token-এ নেই (HTTP 403),
     আর workflow-এ push trigger নেই — তাই schedule-এর অপেক্ষা।

৭.৪  Today/Upcoming match system
     সম্পূর্ণ আলাদা plan (THE_ULTIMATE_PLAN_FILE.txt, ১১০৮ লাইন)। আপনি
     বলেছেন এখন এতে হাত দেব না, তাই দেওয়া হয়নি।


================================================================================
৮. enforcement coverage — সম্পূর্ণ
================================================================================
production-এর প্রতিটি hide site এখন model-এর gate দিয়ে যায়:

  scanner/player_compatibility.py      model_permits_hide 2   audit_hide_safe 0
  scanner/browser_reachability.py      model_permits_hide 2   audit_hide_safe 0
  scanner/fast_pipeline.py             model_permits_hide 3   audit_hide_safe 0
  scanner/bd_verifier.py               model_permits_hide 5   audit_hide_safe 0
  scanner/verifier.py                  model_permits_hide 3   audit_hide_safe 0

পুরোনো `audit_hide_safe` (যা শুধু লিখে রাখত, আটকাতে পারত না) কোথাও অবশিষ্ট
নেই — tests/test_enforcement_coverage.py এই সংখ্যাগুলোর সর্বনিম্ন মান pin
করে রাখে, তাই কেউ gate সরালে test fail করবে।


================================================================================
৯. নিরাপত্তা যাচাই
================================================================================

  stream URL / credential      একটিও বদলায়নি
  data/allowed-hosts.json      অপরিবর্তিত
  site/runtime-config.json     অপরিবর্তিত
  config/settings.json         অপরিবর্তিত
  কোনো channel hide বা promote  করা হয়নি
  ranking / visibility         অপরিবর্তিত
  এই report-এ credential       নেই (যাচাই করা)
  registry-তে raw URL          নেই — শুধু route_id ও public template

আপনার দেওয়া সীমা মেনে চলা হয়েছে: শুধু যে সমস্যাগুলোর কথা বলেছেন সেগুলোই
সারানো হয়েছে, বাইরে কিছু করা হয়নি।


================================================================================
১০. নিজে যাচাই করার উপায়
================================================================================

  CI-র মতো পুরো test      python -m unittest discover -s tests -p "test_*.py"
  ordering                python3 -m pytest tests/test_evidence_ordering.py -v
  health + expiry + slot  python3 -m pytest tests/test_route_preference_gaps.py -v
  enforcement             python3 -m pytest tests/test_enforcement_coverage.py -v
  device ফল               cat reports/zee-device-android_tv.json
  evidence cache          python3 -c "import json;d=json.load(open('state/route-evidence-cache.json'));print(len(d['routes']),'routes')"
  HMAC                    python3 -c "import json;print(json.load(open('reports/visibility-model-audit.json'))['hmac_key'])"
  CI run                  gh run list --limit 6


================================================================================
শেষ কথা
================================================================================

Codex-এর ৬টি পয়েন্টই কোডে গিয়ে যাচাই করা হয়েছে, এবং সবগুলোই কোনো না কোনো
অর্থে সঠিক ছিল।

সবচেয়ে দামি ছিল পয়েন্ট ৪। শুধু line number পড়ে ধরেছে যে আমার gate গুলো
এমন সময়ে চলে যখন model-এর কাছে বিচার করার কিছুই নেই। আমি ৫টি site
enforcement-এ বদলে "সারানো" লিখেছিলাম — gate ছিল, কিন্তু কিছুই আটকাতে পারত
না। এটি এমন একটি ভুল যা test pass করেও থেকে যেত, কারণ test দেখছিল gate
আছে কি না, gate-টা কার্যকর কি না তা নয়। এখন test ক্রমটাই assert করে।

আর device test করতে গিয়ে নিজের একটি ভুল ধরা পড়ল যা আরও খারাপ হতে পারত:
`--profile` শুধু label ছিল, তাই ওভাবে চালালে report-এ "Android/iPhone-এ
test হয়েছে" লেখা হত অথচ কিছুই মাপা হত না। মিথ্যা প্রমাণ তৈরির চেয়ে test না
চালানো ভালো — তাই আগে emulation ঠিক করে তারপর ৬টি session চালানো হলো।

Zee Bangla নিয়ে নতুন দাবি নেই: তিনটি device, তিনটি engine, ৬টি session —
সবেতেই একই decode failure। সমস্যা গঠনগত ও device-নিরপেক্ষ, যা এখন মাপা,
শুধু অনুমান নয়। channel-টি এখনো published, পুরোনো route-এ, এবং registry
proof ধরে রেখেছে।

test suite: 1301 tests, OK (244.781s)

================================================================================
