================================================================================
NEW UPDATE SOLUTION FILE
Codex round-২ + scanner failure — কী সারালাম, প্রমাণ সহ
================================================================================

তারিখ      : 2026-08-26
Repository : DigeeGlamour/click-tv, branch main
commit     : f599d440b
test       : 1284 tests, OK, 0 failure
             (CI-র নিজের command দিয়ে চালানো:
              python -m unittest discover -s tests -p "test_*.py")

দুটো আলাদা কাজ এই file-এ:
  ক) Codex round-২-এ ধরা ৪টি bug — সারানো
  খ) scanner ৮টি run ধরে fail করছিল — কারণ খুঁজে সারানো

খ-টি আগে, কারণ ওটা production ভাঙা ছিল।


================================================================================
১. SCANNER ৮টি RUN ধরে FAIL — কারণ আমার নিজের test ✅ সারানো
================================================================================

--------------------------------------------------------------------------------
১.১  যা দেখা গেল
--------------------------------------------------------------------------------
GitHub Actions-এ টানা ৮টি run failure, সবই একই commit 73ad84dfa-তে:

    completed/failure  73ad84dfa  08-26T18:28  schedule
    completed/failure  73ad84dfa  08-26T18:08  schedule
    completed/failure  73ad84dfa  08-26T16:47  schedule
    ... (মোট ৮টি)

log থেকে আসল কারণ:

    Ran 1260 tests ... FAILED (failures=3)

    FAIL: test_the_original_route_is_preserved_as_a_backup
      AssertionError: 'rgkkw.live' not found in set()
    FAIL: test_the_original_route_matches_the_committed_url_byte_for_byte
      AssertionError: '...rgkkw.live/live/.../98881.ts' not found in []
    FAIL: test_the_primary_is_the_proven_route
      AssertionError: 'verified_global' != 'verified_sustained_playback'

তিনটিই আমার লেখা test, tests/test_zee_bangla_route_fix.py-তে।

--------------------------------------------------------------------------------
১.২  কেন fail করছিল — এবং এখানে system ঠিকই কাজ করেছে
--------------------------------------------------------------------------------
proven jio-tv route এখন **HTTP 530** ফেরত দেয় — তার Cloudflare worker বন্ধ
হয়ে গেছে। সরাসরি মেপে যাচাই করা:

    registry route_id : jio-tv.sm-monirul-islam-rs.workers.dev/live.m3u8?id=625
    source route_id   : jio-tv.sm-monirul-islam-rs.workers.dev/live.m3u8?id=625
    মিলছে?            : True
    reachability      : (530, '')

অর্থাৎ:
  • route source playlist-এ **আছেই**
  • registry-র route_id **হুবহু মিলছে**
  • কিন্তু route নিজে **মরা**

তাই scan সেটা বাদ দিল — নিয়ম মেনে। আর `route_preference` মরা route জোর করে
ফেরায়নি, কারণ তার নিজের নিয়মই তাই: "a route the scanner did not find is a
route this scan cannot vouch for"।

**design ঠিকই কাজ করেছে। ভুল ছিল test-এ।**

--------------------------------------------------------------------------------
১.৩  test-এর নকশাগত ভুল
--------------------------------------------------------------------------------
ওই তিনটি test `data/channels/indian.json`-এর **বর্তমান বিষয়বস্তু** নিয়ে
দাবি করত — কোন host primary, কোনটা backup-এ, status কী।

কিন্তু ওই file **scanner-এর নিজের**। প্রতি run-এ source-গুলো যা উত্তর দেয়
তা থেকে নতুন করে লেখা হয়। কোন route আজ card-এ থাকবে সেটা আজকের network
সম্পর্কে scanner-এর উত্তর — test-এর কাজ সেটা জমিয়ে রাখা নয়।

সংশোধন: test এখন যা যাচাই করে, তা এই কাজের নিজের অধীনে:
  • report-এ লেখা proof (দুটি পূর্ণ ১২০s PASS)
  • দুটি durable registry (sustained_proof, route_preference)
  • promotion script-এর guard গুলো
  • card-এর একটিমাত্র invariant যা network-নির্ভর নয়:
    **catalogue-এ উপস্থিত channel hidden নয়**

--------------------------------------------------------------------------------
১.৪  একই ঝুঁকি আরেক জায়গায় ছিল, সেটাও সারানো
--------------------------------------------------------------------------------
tests/test_promote_proven_channels.py ওই ৭টি restored channel-এর জন্য দাবি
করত প্রতিটি "ঠিক একবার" catalogue-এ থাকবে। অর্থাৎ ওই ৭টির **যেকোনো একটির**
source মরলেই CI আবার ভাঙত — একই bug, ৭ বার অপেক্ষা করছিল।

এখন দাবি: "একবারের বেশি নয়" (duplication রোধ, যা restoration সত্যিই
নিশ্চিত করে) + registry-তে proof টিকে আছে কি না।

--------------------------------------------------------------------------------
১.৫  যাচাই
--------------------------------------------------------------------------------
CI ঠিক যে command চালায়, সেটাই চালানো হলো:

    python -m unittest discover -s tests -p "test_*.py"
    Ran 1284 tests ... OK

সৎ সীমা: GitHub Actions-এ নতুন run নিজে trigger করা যায়নি
(workflow_dispatch-এ HTTP 403 — token-এর অনুমতি নেই), আর workflow-এ `push`
trigger নেই, শুধু `schedule`। তাই fix commit-এ CI নিজে চলার প্রমাণ এখনো
নেই — cron-এ চলবে। উপরের local যাচাই CI-র হুবহু একই command, কিন্তু
"CI-তে pass করেছে" বলছি না, কারণ সেটা এখনো ঘটেনি।


================================================================================
২. bd_verifier ও verifier.py-র HIDE SITE MODEL মানত না ✅ সারানো
================================================================================
Codex ধরেছিল, এবং কোডে গিয়ে যাচাই করে সত্যি পাওয়া গেল।

আগের পর্বে "safe enforcement" বসানো হয়েছে বলে দাবি করেছিলাম, কিন্তু সেটা
বসেছিল মাত্র ৩টি ফাইলের ৫টি site-এ। bd_verifier.py-র **৫টি** ও
verifier.py-র **৩টি** site তখনো `audit_hide_safe` (শুধু record করে, কখনো
আটকায় না) ব্যবহার করত।

আরও গুরুতর: `verify_bd_stream` (bd_verifier-এর hide function) fast_pipeline-এ
evidence সরবরাহের **আগেই** ডাকা হয় — line ~1402/1771/1875, আর evidence
সরবরাহ ~1900-এ। তাই একটি channel player_compatibility-র path-এ model-এর
সুরক্ষা পেলেও, bd_verifier-এর ভিন্ন যাচাইয়ে **কোনো গেট ছাড়াই** hide হয়ে
যেতে পারত। সুরক্ষা নির্ভর করত কোন code path আগে channel-টিকে ধরছে তার উপর।

সংশোধন — এখন প্রতিটি production hide site enforcement-এ:

  scanner/player_compatibility.py         audit-only 0   enforced 2
  scanner/browser_reachability.py         audit-only 0   enforced 2
  scanner/fast_pipeline.py                audit-only 0   enforced 3
  scanner/bd_verifier.py                  audit-only 0   enforced 5
  scanner/verifier.py                     audit-only 0   enforced 3

  মোট: ১৫টি site, ১৫টিই enforced, ০টি audit-only

একটি site (`verifier.resolution_policy`) শর্তসাপেক্ষে hide করে
(`if not policy_ok`), তাই model আটকালে `policy_ok = True` করা হয়েছে যাতে
পরের লাইনগুলো সঠিকভাবে publish_allowed/verified সেট করে।

test: tests/test_enforcement_coverage.py (২টি) — প্রতিটি ফাইলের জন্য
সর্বনিম্ন কতগুলো enforcement call থাকা উচিত তা explicit লেখা, আর নিশ্চিত
করে কোনো ফাইলে audit-only call বাকি নেই। ভবিষ্যতে নতুন hide site গেট ছাড়া
যোগ হলে এই test fail করবে।


================================================================================
৩. PROVEN ROUTE ৭ নম্বরে থাকলে বাদ পড়ত ✅ সারানো
================================================================================
merger.py-তে ranking প্রথমে সর্বোচ্চ ৬টি route বেছে নেয় (`max_total=6`),
**তারপর** `promote_preferred` চলত। তাই ৭টি বা বেশি source থাকলে এবং proven
route ৭ম বা পরে থাকলে, promote_preferred সেটা কখনো দেখতেই পেত না।

Reproduce করে যাচাই (৬টি rival + proven route ৭ম):

    fix-এর আগে : primary = rival0 (ভাঙা)   → proven route primary? False
    fix-এর পরে : primary = good.example.net → proven route primary? True

সংশোধন: `promote_preferred`-এ নতুন `full_pool` parameter — truncation-এর
আগের পুরো candidate pool। proven route truncated selection-এ না থাকলেও
full_pool-এ থাকলে টেনে আনা হয়, আর caller-এর slot সীমা রক্ষার জন্য সবচেয়ে
দুর্বল entry বাদ পড়ে। merger.py `protocol_candidates` পাস করে।

test: tests/test_route_preference_gaps.py-র SlotTruncationTests (৬টি) —
পুরোনো ভুল আচরণও pin করা আছে, যাতে fix-টা তুলনায় দৃশ্যমান থাকে; শেষ test
সরাসরি real merger দিয়ে Codex-এর ঠিক সেই scenario চালায়।


================================================================================
৪. PREFERRED ROUTE-এর EXPIRY ছিল না ✅ সারানো
================================================================================
route_preference.py-তে কোনো timestamp ছিল না। একবার proven হলে route
ভবিষ্যতে নষ্ট হয়ে গেলেও registry চিরকাল সেটাকেই primary বানাতে চাইত।

সংশোধন:
  • প্রতিটি record-এ `recorded_at`
  • `PREFERENCE_TTL_SECONDS = 14 দিন`
  • timestamp-হীন পুরোনো entry stale ধরা হয়, চিরস্থায়ী নয়
  • re-record করলে TTL রিফ্রেশ

কেন ১৪ দিন, এবং কেন Phase 0b-র lock নয়: persistence.ttl_seconds = 1800s
একটি **ভিন্ন** mechanism-এর (escalation counter) জন্য। channel scan বাস্তবে
~৬ ঘণ্টা অন্তর চলে, movie ~৪৮ ঘণ্টা — ১৮০০ সেকেন্ড এত ছোট যে পরের scan
আসার আগেই মেয়াদ শেষ হত। ১৪ দিন ওই cadence-এর অনেক গুণ। এটি নতুন, ইচ্ছাকৃত
policy decision — লুকানো অনুমান নয়, দুটি module-এর docstring-এ লেখা।

test: StalenessTests (৬টি)


================================================================================
৫. EVIDENCE PIPELINE দুই VANTAGE ব্যবহার করত না, PERSIST করত না ✅ সারানো
================================================================================
আগের file-এ দাবি করেছিলাম pipeline scanner-এর সঙ্গে যুক্ত। যুক্ত ছিল,
কিন্তু **অসম্পূর্ণভাবে**, দুই দিকেই:

  ক) scanner ইতিমধ্যে proxy vantage-এর ফল রাখে (`proxy_http_status`,
     bd_verifier-এর নিজের proxy check থেকে) — কিন্তু `_supply_scan_evidence`
     সেটা কখনো পড়ত না। ফলে দুটো record-ই একই vantage-এর, আর
     `vantages_are_independent` কখনো True হতে পারত না।

  খ) `_EVIDENCE` ছিল একটি সাধারণ Python dict, process-এর memory-তে। CI-তে
     প্রতি scan আলাদা process, তাই প্রতি scan শুরু হত খালি dict দিয়ে।
     অর্থাৎ "দুই আলাদা time window" শর্ত **কখনোই পূরণযোগ্য ছিল না** —
     ভুল configuration নয়, structural impossibility।

সংশোধন: নতুন scanner/route_evidence_cache.py — record disk-এ persist করে
(state/route-evidence-cache.json), route_id ধরে, pruned। ০ মান proxy-র
sentinel ("কোনো check হয়নি") ধরে বাদ দেওয়া হয়, real status নয়।

মাপা প্রমাণ — সত্যিকারের দুই scan, ৬ ঘণ্টা ব্যবধানে, দুই vantage,
প্রকৃত route failure:

    scan ১ (t=0)         → cache-এ লেখা হলো
    scan ২ (t + ৬ ঘণ্টা)  → cache থেকে scan ১-এর record পড়া গেল
                            supports_hide       = True
                            distinct_windows    = 2
                            independent_vantages = True

উল্টো দিকও যাচাই: শুধু bare transport status (503 alone, browser test ছাড়া)
দিয়ে একই জিনিস চালালে supports_hide = False — এমন observation global
scope-ই পায় না। persistence শুধু কী **combine** করা যায় সেটা বদলায়, model
কী **accept** করে সেটা নয়।

test: tests/test_route_evidence_cache.py (১০টি)


================================================================================
৬. TEST ISOLATION — একটি bug এই কাজেই ধরা পড়ল ✅ সারানো
================================================================================
`_supply_scan_evidence` default cache path-এ লেখে। তাই
tests/test_scan_evidence_supply.py-র দুটি test আসল
state/route-evidence-cache.json পড়ছিল এবং এই session-এর manual testing-এ
লেখা leftover data দেখে fail করছিল (`4 != 0`)।

সংশোধন: ওই test class এখন tempdir-এ isolate করা, `DEFAULT_PATH` monkeypatch
করে। leftover artifact-ও মুছে দেওয়া হয়েছে।


================================================================================
৭. ZEE BANGLA-র বর্তমান অবস্থা — সৎভাবে
================================================================================
  card primary : rgkkw.live (mpegts)
  card status  : verified_global
  backups      : 0

অর্থাৎ card পুরোনো ভাঙা raw-TS route-এ ফিরে গেছে। কারণ: proven route মরে
গেছে (HTTP 530)।

নতুন করে খোঁজা হলো — ৮টি সত্যিকারের Zee Bangla candidate
(Cinema/Sonar বাদ দিয়ে, আপনার নিয়ম মেনে):

    Zee Bangla HD    stream.ottplus.bd                       403
    Zee Bangla HD    live.shamim-iptv.workers.dev            404
 ✅ ZEE BANGLA       103.159.180.34                          200  hls
    Zee Bangla       edge2.roarzone.net                      403
    Zee Bangla       stream.ottplus.bd                       403
    Zee Bangla       bldcmprod-cdn.toffeelive.com            URLError
    Zee Bangla       jio-tv.sm-monirul-islam-rs.workers.dev  530
    Zee Bangla HD    jio-tv.sm-monirul-islam-rs.workers.dev  530

মাত্র ১টি জীবিত। তাতেও ১২০s browser test চালানো হলো:
    proven: 0 / 1
    a0s0 proxy   advisory:transient_network         hls networkError/manifestLoadError
    a0s1 proxy   advisory:transient_network         hls networkError/manifestLoadError
    a1s0 proxy   advisory:transient_network         hls networkError/manifestLoadError
    a1s1 proxy   advisory:transient_network         hls networkError/manifestLoadError

proxy route-এ manifest load হয় না, তাই PASS floor ছোঁয়নি।

**সিদ্ধান্ত: এই মুহূর্তে Zee Bangla-র কোনো proven route নেই।** এটি লুকাচ্ছি
না এবং "সারানো হয়েছে" বলছি না। registry অটুট আছে (route_id সহ) — jio-tv
route ফিরে এলে merger আবার সেটাকে primary করবে, কোনো code পরিবর্তন ছাড়াই।
সেটাই route_preference-এর উদ্দেশ্য ছিল, এবং এই ঘটনাই প্রমাণ করে ওটা
মরা route জোর করে ফেরায় না।

registry অবস্থা:
  route-preference : 1 entry
    Zee Bangla     2 PASS, window 120.0s
  sustained-proof  : 8 entry


================================================================================
৮. এই পর্বে যা যোগ/বদল হলো
================================================================================

নতুন code:
  scanner/route_evidence_cache.py        cross-scan evidence persistence

বদলানো code:
  scanner/bd_verifier.py                 ৫টি site enforcement-এ
  scanner/verifier.py                    ৩টি site enforcement-এ
  scanner/fast_pipeline.py               বাকি ২টি site + proxy vantage + cache
  scanner/merger.py                      full_pool pass
  scanner/route_preference.py            full_pool support + TTL/staleness

নতুন test:
  tests/test_enforcement_coverage.py      ২টি
  tests/test_route_preference_gaps.py    ১২টি
  tests/test_route_evidence_cache.py     ১০টি

সংশোধিত test (CI ভাঙা বন্ধ):
  tests/test_zee_bangla_route_fix.py     generated data-র দাবি সরানো
  tests/test_promote_proven_channels.py  একই ঝুঁকি সরানো
  tests/test_scan_evidence_supply.py     tempdir-এ isolate

স্পর্শ করা হয়নি (আপনার নিজের uncommitted কাজ):
  config/sources/tv.json
  tests/test_final_scanner_contract.py


================================================================================
৯. এখনো যা বাকি
================================================================================

৯.১  CI-তে fix commit-এর নিজে-চলা প্রমাণ
     workflow_dispatch-এ 403 (অনুমতি নেই), workflow-এ push trigger নেই।
     cron-এ চলবে। local-এ CI-র হুবহু command দিয়ে যাচাই করা: 1284 OK।

৯.২  Zee Bangla-র জন্য একটি জীবিত proven route
     ৮টি candidate-এর ৭টি মরা, ১টি PASS করেনি। নতুন source যোগ হলে বা
     jio-tv worker ফিরলে registry নিজেই কাজ করবে।

৯.৩  ২১১টি movie — BD/residential vantage আমার হাতে নেই

৯.৪  Android/iPhone/TV playback test — এখানেই চালানো সম্ভব, বললে করব

৯.৫  Today/Upcoming match system — সম্পূর্ণ আলাদা plan
     (THE_ULTIMATE_PLAN_FILE.txt, ১১০৮ লাইন), আপনি বলেছেন এখন ধরব না


================================================================================
১০. নিরাপত্তা যাচাই
================================================================================

  stream URL/credential      একটিও বদলায়নি
  data/allowed-hosts.json    অপরিবর্তিত
  site/runtime-config.json   অপরিবর্তিত
  config/settings.json       অপরিবর্তিত
  কোনো channel hide/promote   না
  committed report-এ credential  নেই (যাচাই করা)


================================================================================
১১. নিজে যাচাই করার উপায়
================================================================================

  CI-র মতো test:          python -m unittest discover -s tests -p "test_*.py"
  enforcement coverage:    python3 -m pytest tests/test_enforcement_coverage.py -v
  ৭ম-route bug:            python3 -m pytest tests/test_route_preference_gaps.py -v
  cross-scan evidence:     python3 -m pytest tests/test_route_evidence_cache.py -v
  registry:                cat state/route-preference.json
  scanner run:             gh run list --limit 5


================================================================================
শেষ কথা
================================================================================

এই পর্বের সবচেয়ে জরুরি শিক্ষা: **scanner-এর নিজের generated data নিয়ে test
দাবি করা যাবে না।** ওই ফাইল প্রতি run-এ নতুন হয়, আর network বদলালে বিষয়বস্তু
বদলাবেই। আমি তিনটি test-এ সেটা করেছিলাম, আর যখন proven route-টি মরে গেল
(HTTP 530) তখন system ঠিকভাবে সেটা বাদ দিল — এবং আমার test সেই সঠিক
আচরণকেই failure বলে ৮টি scanner run নামিয়ে দিল।

Codex-এর ধরা ৪টি bug-ও আসল ছিল, বিশেষত দুটি: hide site গুলোর অর্ধেক model
মানত না (সুরক্ষা নির্ভর করত কোন path আগে চলছে তার উপর), আর evidence
process-এর memory-তে থাকায় দুই-window শর্ত structurally অসম্ভব ছিল।

Zee Bangla নিয়ে নতুন কোনো দাবি করছি না — proven route মরে গেছে, আর সেটাই
লেখা আছে।

================================================================================
