{
  "pipeline": "upcoming",
  "description": "Deliberately empty. All eleven event feeds are registered once in today-match.json, because nine of them carry live and not-started fixtures in the same file and each record's own status decides its tab - scanner/events.py routes on schedule status, not on which file a source was configured in. Listing them here as well made every source fetch and parse twice and doubled the candidate pool.",
  "sources": []
}
